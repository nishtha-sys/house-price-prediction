# 🏠 House Price Prediction

Performed EDA and feature selection on 21,600+ property records (King County dataset); trained and evaluated a Linear Regression model (R²: 0.52, MAE: $173.8K), then documented specific next-step improvements (additional features, non-linear models) — demonstrating iterative model evaluation and reporting discipline.

**🔗 Live app:** [nishtha-house-price-prediction.streamlit.app](https://nishtha-house-price-prediction.streamlit.app/)

---

## Overview

This project trains a Linear Regression model on the [King County House Sales dataset](https://www.kaggle.com/datasets/harlfoxem/housesalesprediction) and serves predictions through a simple Streamlit interface — enter a few property details, get an instant price estimate.

> **Note:** The model is trained on the King County (Seattle, USA) housing dataset, so predictions are in USD. An approximate USD → INR conversion (₹83/$) is applied for the INR display — this is a currency conversion, not an India-specific pricing model.

## Features

- Predicts house price from 5 key inputs: living area, bedrooms, bathrooms, floors, and neighborhood living area (`sqft_living15`)
- Displays predicted price in both USD and INR
- Interactive, single-page Streamlit UI
- Linear Regression model trained with scikit-learn
- One-command local setup and one-click cloud deployment

## Model Performance

| Metric | Value |
|---|---|
| MAE (Mean Absolute Error) | ~$173,800 |
| R² Score | ~0.52 |

*Trained on an 80/20 train-test split of ~21,600 King County home sales records.*

## Tech Stack

- **Python**
- **Streamlit** — web UI
- **scikit-learn** — Linear Regression model
- **pandas / numpy** — data handling
- **joblib** — model serialization

## Project Structure

house-price-prediction/
├── app.py # Streamlit app — loads model, takes input, shows prediction
├── train_model.py # Trains the Linear Regression model from raw data
├── house_price_model.pkl # Pre-trained model (generated by train_model.py)
├── kc_house_data.csv # King County house sales dataset
├── requirements.txt # Python dependencies
└── README.md

## Running Locally

1. Clone the repo:
```bash
   git clone https://github.com/nishtha-sys/house-price-prediction.git
   cd house-price-prediction
```

2. Install dependencies:
```bash
   pip install -r requirements.txt
```

3. (Optional) Retrain the model from scratch:
```bash
   python train_model.py
```

4. Run the app:
```bash
   streamlit run app.py
```

The app will open at `http://localhost:8501`.

## Deployment

Deployed on [Streamlit Community Cloud](https://share.streamlit.io) — connected directly to this repo's `main` branch, with `app.py` as the entry point.

## Future Improvements

- Add more features (zipcode, condition, grade, waterfront) for better accuracy
- Try non-linear models (Random Forest, XGBoost) and compare performance
- Add input validation and confidence intervals on predictions
- Add data visualizations (price distribution, feature correlations)

## Author

**Nishtha Sahani**
[Portfolio](https://portfolio-nishthasahani.vercel.app) · [GitHub](https://github.com/nishtha-sys)

